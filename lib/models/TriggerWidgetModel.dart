import 'package:flutter/cupertino.dart';

class TriggerWidgetModel {
  String? questionTag;
  String? questionType;
  String? helpText;
  Widget? widget;

  TriggerWidgetModel({this.questionTag, this.questionType, this.helpText, this.widget});
}